# manifest-registry config

manifest.registry.url=${manifest.registry.url}

# validate incoming xml document with schema (to avoid version conflict and xml errors)
manifest.registry.schema.validation=false
